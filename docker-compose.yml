services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: riu-frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  frontend_node_modules:
