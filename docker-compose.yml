services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: riu-frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true
